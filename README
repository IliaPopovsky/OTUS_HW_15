Выданный для проверки файл oscar.sql формата sql необходимо конвертировать в файл формата SQLite3 с помощью специальной программы. Например, DB Browser for SQLite (Файл->Импорт->База данных из файла SQL...->Open->Вводим название новой базы данных в формате SQLite3->Save->OK->X(Закрываем окно программы DB Browser for SQLite)->Save). Потом, уже конвертированный в формат SQLite3 файл мы используем в работе нашей программы. При конвертировании мы можем ставить целевому файлу расширение .sqlite, .sqlite3, а можем вообще не ставить никакого расширения. При конвертации размер файла вырос с 6,6 kB до 8,2 kB.

The oscar.sql file of sql format issued for verification must be converted to a file of SQLite3 format using a special program. For example, DB Browser for SQLite (File->Import->Database from SQL file...->Open->Enter the name of the new database in SQLite3 format->Save->OK->X(Close the DB Browser for SQLite program window)->Save). Then, the file already converted to SQLite3 format is used in the work of our program. When converting, we can set the target file extension .sqlite, .sqlite3, or we can not set any extension at all. During the conversion, the file size increased from 6.6 kB to 8.2 kB.
